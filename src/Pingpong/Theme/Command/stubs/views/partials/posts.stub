@if($posts->count())
	<hr>
	<div class="media">
	@foreach($posts as $post)
		<div class="media-body">
			<a href="{{ $post->permalink }}" class="media-heading">
				<h4>
					{{ $post->title }}
				</h4>
			</a>
			<div>
				{{ $post->content }}
				<a href="{{ $post->permalink }}">Read More</a>
			</div>
		</div>
	@endforeach
	</div>
	<div class="text-center posts-pagination">
		{{ paginator_links($posts)}}
	</div>
@else
	There are no posts.
@endif