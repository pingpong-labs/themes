@extends('{{name}}::layouts.master')

@section('content')
	<h3>
		Search results for : {{ $q }}
	</h3>
	@if($posts->count())
		@include('{{name}}::partials.posts')
	@else
		<div class="alert alert-info">
			<p>
				Nothing found for <b>{{ $q }}</b>
			</p>
		</div>
	@endif

@endsection